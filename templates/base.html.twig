<!DOCTYPE html>
<html lang="en-gb">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('/images/favicon/apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('/images/favicon/favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('/images/favicon/favicon-16x16.png') }}">
    <link rel="manifest" href="{{ asset('/images/favicon/site.webmanifest') }}">
    <link rel="mask-icon" href="{{ asset('/images/favicon/safari-pinned-tab.svg" color="#5bbad5') }}">
    <link rel="shortcut icon" href="{{ asset('/images/favicon/favicon.svg') }}">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="msapplication-config" content="{{ asset('/images/favicon/browserconfig.xml') }}">
    <meta name="theme-color" content="#ffffff">
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap"
          rel="stylesheet">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
<header class="bg-brand-grey">
    {% block header %}
        <div class="container mx-auto py-4 flex items-center justify-center md:place-content-between px-4">
            <img src="{{ asset('images/phphants.png') }}" alt="PHP Hampshire" width="276">
            <a href="{{ url('app_login') }}" target="_blank"
               class="bg-black bg-opacity-25 text-white text-bold px-16 py-2 uppercase font-semibold rounded font-sans-200 hidden md:block">Log
                In</a>
        </div>
        <div class="h-[50vh] md:h-[40vh] text-white bg-hero-image bg-cover bg-center flex">
            <div class="text-container bg-gray-800 min-w-100 flex-1 bg-opacity-75 flex flex-col justify-center items-center text-center p-4">
                <h1 class="font-semibold text-6xl pb-6">Welcome to PHP Hants</h1>
                <p class="text-xl max-w-3xl">PHP Hants is a monthly meetup aiming to help people learn, share
                    knowledge and support the PHP & Tech community in Hampshire and the surrounding areas.</p>
                <a href="https://www.meetup.com/meetup-group-phphants/" target="_blank"
                   class="bg-meetup-pink text-white tracking-wide px-16 py-2 uppercase rounded font-sans-200 mt-6">I
                    want to attend!</a>
            </div>
        </div>
    {% endblock %}
</header>
<main {% block main_attributes %}{% endblock %}>
    {% block body %}{% endblock %}
</main>
<footer class="text-right px-4 bg-neutral-800">
    <div class="container mx-auto py-2">
    <p class="text-sm text-white">&copy; PHP Hants {{ 'now'|date('Y') }}</p>
    </div>
</footer>
</body>
</html>
